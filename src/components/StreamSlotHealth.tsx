/**
 * A stream can fail while its provider is still resolving an HLS URL. Keep it in
 * its grid slot so the user can retry it instead of silently losing the choice.
 */
export const StreamSlotHealth = () => null;
